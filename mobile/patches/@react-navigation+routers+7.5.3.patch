diff --git a/node_modules/@react-navigation/routers/lib/module/TabRouter.js b/node_modules/@react-navigation/routers/lib/module/TabRouter.js
index 3a558c4..9ef0c18 100644
--- a/node_modules/@react-navigation/routers/lib/module/TabRouter.js
+++ b/node_modules/@react-navigation/routers/lib/module/TabRouter.js
@@ -118,12 +118,18 @@ export default function TabRouter({
       routeNames,
       routeParamList
     }) {
-      const state = partialState;
+      const state = partialState ?? {
+        index: 0,
+        routes: [],
+        history: [],
+        preloadedRouteKeys: []
+      };
       if (state.stale === false) {
         return state;
       }
+      const safeRoutes = Array.isArray(state.routes) ? state.routes : [];
       const routes = routeNames.map(name => {
-        const route = state.routes.find(r => r.name === name);
+        const route = safeRoutes.find(r => r.name === name);
         return {
           ...route,
           name,
@@ -134,7 +140,7 @@ export default function TabRouter({
           } : route ? route.params : undefined
         };
       });
-      const index = Math.min(Math.max(routeNames.indexOf(state.routes[state?.index ?? 0]?.name), 0), routes.length - 1);
+      const index = Math.min(Math.max(routeNames.indexOf(safeRoutes[state?.index ?? 0]?.name), 0), routes.length - 1);
       const routeKeys = routes.map(route => route.key);
       const history = state.history?.filter(it => routeKeys.includes(it.key)) ?? [];
       return changeIndex({
