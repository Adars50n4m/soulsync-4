{
  "name": "soulsync-upload-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SoulSync media uploads to R2",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "wrangler": "^3.22.0"
  }
}
